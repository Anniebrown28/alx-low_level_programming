#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: first number to multiply
 * @b: second numberto multiply with first number
 * Return: multiplication.
 */
int mul(int a, int b)
{
	return (a * b);
}
